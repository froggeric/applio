{
  "planPath": "docs/superpowers/plans/2026-08-21-a11y-phase3-fork-local-hardening.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: applio_i18n locale-glob + JSON guards",
      "status": "completed",
      "description": "**Goal:** Make `NativeI18n`'s system-locale fallback match upstream `assets/i18n/i18n.py`'s prefix-glob semantics (any available language starting with the 2-letter prefix wins) and survive corrupt (non-dict) language/overrides JSON without `AttributeError`.\n\n**Files:**\n- Modify: `applio_i18n.py:42-61` (both JSON loads), `applio_i18n.py:63-86` (`_resolve_from_config`)\n- Test: `tests/test_applio_i18n.py` (extend; 5 → 8 tests)\n\n**Acceptance Criteria:**\n- [ ] With system locale `xx_ZZ` and only `xx_YY.json` present (no override set), `NativeI18n` resolves `language == \"xx_YY\"` (prefix-glob, mirroring upstream `i18n.py:24-30`).\n- [ ] Dash-form locales normalize (`fr-FR` → prefix `fr`) before matching.\n- [ ] A `languages/<locale>.json` containing a JSON list (not object) is skipped gracefully: `language` falls back to `en_US` and `native_tr(key) == key` — no `AttributeError`.\n- [ ] A list-valued `applio_i18n_overrides.json` (or a list-valued per-locale layer inside it) is skipped the same way.\n- [ ] Existing 5 tests still pass; suite prints `(8)`.\n\n**Verify:** `venv_macos/bin/python tests/test_applio_i18n.py` → `All applio_i18n tests passed (8).`\n\n```json:metadata\n{\"files\": [\"applio_i18n.py\", \"tests/test_applio_i18n.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_applio_i18n.py\", \"acceptanceCriteria\": [\"prefix-glob resolves xx_ZZ -> xx_YY\", \"corrupt locale JSON falls back to en_US without crash\", \"corrupt overrides JSON skipped\", \"suite prints (8)\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 2: Scope healRecordToggles to record section",
      "status": "completed",
      "description": "**Goal:** Stop stamping `aria-pressed` on every Start/Stop-labeled button in the document; anchor it to the realtime record toggle's container via the fork-injected `#browse-record_audio_path` Browse button, so the momentary engine Start/Stop pair (`tabs/realtime/realtime.py:941-942`) is never mislabeled as a toggle.\n\n**Files:**\n- Modify: `assets/applio_a11y.js:103-110` (`healRecordToggles`; call sites at 167 and 308 unchanged)\n- Test: `tests/test_a11y_js_invariants.py` (NEW — source-invariant tests for the JS payload; no browser harness exists in-repo)\n\n**Scope selector — verified against the installed gradio 6.20.0 bundle:** scope = `anchor.closest(ACCORDION_BLOCK)` (the live-verified `.gr-accordion` container). NOT `.form`/`gradio-column`: `gradio-column` does not exist (only `gradio-app` is a custom element; Column renders `div.column`), and `gr.Button` is not a FormComponent so `.form` scopes are wrong/unreliable from the anchor. The record toggle is a SIBLING of the path field's Column inside the `Record Audio (Optional)` accordion; the engine Start/Stop pair sits at the top of the Blocks, outside every accordion.\n\n**Acceptance Criteria:**\n- [ ] `healRecordToggles` resolves `#browse-record_audio_path`, scopes to `anchor.closest(ACCORDION_BLOCK)`, and only stamps buttons inside that scope.\n- [ ] The function no longer calls document-wide `querySelectorAll(\"button\")`.\n- [ ] When the anchor (or its accordion) is absent (page without the realtime tab mounted), the function is a no-op.\n- [ ] New suite passes: `All a11y JS invariant tests passed (1).` (Task 4 adds a second test.)\n\n**Verify:** `venv_macos/bin/python tests/test_a11y_js_invariants.py` → `All a11y JS invariant tests passed (1).`\n\n```json:metadata\n{\"files\": [\"assets/applio_a11y.js\", \"tests/test_a11y_js_invariants.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_a11y_js_invariants.py\", \"acceptanceCriteria\": [\"healRecordToggles anchored on #browse-record_audio_path, scoped via ACCORDION_BLOCK (.gr-accordion)\", \"no document-wide button scan\", \"no-op when anchor/accordion absent\", \"suite (1) passes\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 3: Unify terminal-words map",
      "status": "pending",
      "description": "**Goal:** Eliminate the duplicated `word_key → status` mapping (`applio_progress_api._collect_words` vs `applio_launcher._a11y_terminal_words`) — one AppKit-free helper in `applio_progress_api.py` becomes the single source of truth for the `f\"{type}:{name}\"` word_key format.\n\n**Files:**\n- Modify: `applio_progress_api.py:139-151` (`_collect_words` → delegation; new `terminal_words_from_history`), `applio_launcher.py:5406-5427` (`_a11y_terminal_words` → delegation)\n- Test: `tests/test_progress_api.py` (extend; 7 → 8 tests)\n\n**Acceptance Criteria:**\n- [ ] `terminal_words_from_history(entries)` exists in `applio_progress_api.py`: skips entries missing any of type/model_name/status (and non-dict entries), `setdefault` (newest-first wins), `entries=None` → `{}`.\n- [ ] Both `_collect_words` and the launcher's `_a11y_terminal_words` delegate to it (no duplicated loop remains in either file).\n- [ ] Payload `words` for well-formed history entries is byte-identical to before; JS fallback for absent keys unchanged (`assets/applio_a11y.js:242`).\n- [ ] Accepted behavior change: `_collect_words` previously defaulted missing fields to `process`/`job`/`completed`; it now skips incomplete entries (strictness improvement — JS already falls back to \"finished\"). Update any test asserting the old defaulting.\n- [ ] Suite prints `(8)`.\n\n**Verify:** `venv_macos/bin/python tests/test_progress_api.py` → `All progress API tests passed (8).`\n\n```json:metadata\n{\"files\": [\"applio_progress_api.py\", \"applio_launcher.py\", \"tests/test_progress_api.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_progress_api.py && venv_macos/bin/python tests/test_applio_a11y.py\", \"acceptanceCriteria\": [\"terminal_words_from_history shared helper exists + tested\", \"both call sites delegate\", \"suite (8) + a11y (17) pass\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 4: Failure log tails in web payload",
      "status": "pending",
      "blockedBy": [
        1,
        2
      ],
      "description": "**Goal:** When a job fails, the web \"Last result\" region shows the last log lines (Phase 2 deferral \"error surfacing with full log tails\" — fork-side portion): the progress route exposes `payload.errors` (bounded), and the JS appends the tail to the persisted terminal text. Spoken announcements stay short (no log spam in the live region).\n\n**Files:**\n- Modify: `applio_progress_api.py:118-127` (`build_progress_payload` gains `errors` param), `:203+` (`handle_progress` computes it), new `_recent_error_tails` near `read_log_tail` (`:31-40`)\n- Modify: `assets/applio_a11y.js` — terminal push sites (lines ~240-259) + persist loop (~281-283)\n- Test: `tests/test_progress_api.py` (8 → 11), `tests/test_a11y_js_invariants.py` (1 → 2)\n\n**Acceptance Criteria:**\n- [ ] `payload[\"errors\"]`: list of at most 2 entries, newest-first, only statuses `failed`/`error`, each `{type, name, status, tail}` with `tail` ≤ 1200 chars (seek-based `read_log_tail(path, max_bytes=4096)` then `[-1200:]`); missing log file → empty `tail`, entry still listed.\n- [ ] Live/running jobs still never carry a tail in `jobs` (the unconditional `log_tail` strip stays).\n- [ ] `build_progress_payload` remains pure — errors passed as a parameter.\n- [ ] JS: terminal announcements push a third tuple element (bounded tail suffix, `\"\"` when N/A); `persistResult` writes `message + \" — \" + tail`; the spoken `announce()` path is unchanged; DOM writes remain `textContent`-only.\n- [ ] Both suites pass with updated counts (`(11)` and `(2)`).\n\n**Verify:** `venv_macos/bin/python tests/test_progress_api.py && venv_macos/bin/python tests/test_a11y_js_invariants.py` → `(11)` and `(2)` pass lines.\n\n```json:metadata\n{\"files\": [\"applio_progress_api.py\", \"assets/applio_a11y.js\", \"tests/test_progress_api.py\", \"tests/test_a11y_js_invariants.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_progress_api.py && venv_macos/bin/python tests/test_a11y_js_invariants.py\", \"acceptanceCriteria\": [\"payload.errors bounded (2 entries, 1200 chars)\", \"live jobs never carry tails\", \"JS persistResult appends tail, announce unchanged\", \"suites (11)+(2) pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 5: Blur-time path validation (13 fields)",
      "status": "pending",
      "description": "**Goal:** Typed paths get validated when the field loses focus (Phase 2 deferral \"typed-path on-change validation for the remaining fields\"): `~` expands (field self-heals), missing paths and wrong types warn via `gr.Warning` (the announced toast channel), wired by the Browse patcher next to every field it already touches.\n\n**Files:**\n- Modify: `applio_browse_ui.py` (new `_make_validator` + `attach_path_validation`)\n- Modify: `patches/patch_browse_buttons.py:119-122` (generated-line template gains the validation call)\n- Test: `tests/test_browse_ui.py` (5 → 9), `tests/test_patch_fixtures.py` (assertion extension; count stays 5)\n\n**Acceptance Criteria:**\n- [ ] `attach_path_validation(target, mode)` wires `target.blur(fn=_make_validator(mode), inputs=[target], outputs=[target])` — works for `gr.Textbox` AND `gr.Dropdown` targets (both list `Events.blur` in gradio 6.20.0 — textbox.py:65 / dropdown.py:44; the 13 fields include both kinds). A construction-time wiring test proves the API on both component kinds.\n- [ ] Validator: empty/blank → unchanged, no warning; `~` → expanded value returned (self-heal); nonexistent → `gr.Warning(\"Path does not exist: …\")` + expanded value; `mode=\"folder\"` on a file / `mode in (\"file\",\"pth\")` on a directory → `gr.Warning(\"Not a folder…\"/\"Not a file…\")`.\n- [ ] The patcher's injected block per field contains both lines (browse button + validation attach) for all 13 fields; fixture test asserts `attach_path_validation(` once per field var.\n- [ ] INVARIANT: the injected validation line must NOT contain the substring `_applio_browse_` — `tests/test_patch_fixtures.py:115` asserts `patched.count(\"_applio_browse_\") == len(fields)`.\n- [ ] Existing 5 browse tests still pass; suite prints `(9)`.\n\n**Verify:** `venv_macos/bin/python tests/test_browse_ui.py && venv_macos/bin/python tests/test_patch_fixtures.py` → `(9)` and `(5)`.\n\n```json:metadata\n{\"files\": [\"applio_browse_ui.py\", \"patches/patch_browse_buttons.py\", \"tests/test_browse_ui.py\", \"tests/test_patch_fixtures.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_browse_ui.py && venv_macos/bin/python tests/test_patch_fixtures.py\", \"acceptanceCriteria\": [\"validator expands ~, warns on missing/wrong type\", \"wiring proven on Textbox AND Dropdown (.blur)\", \"all 13 fields get attach_path_validation via patcher, no _applio_browse_ substring in the new line\", \"suites (9)+(5) pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 6: Patcher anchor-miss exit codes",
      "status": "pending",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** A post-upstream-sync anchor miss can never again silently skip a patcher: every `patch_*.py` exits `2` on anchor miss, and `pre_build_patch()` in `build_macos.py` fails the build on ANY nonzero patcher exit, listing all failures. Usage guards keep `1`; file-not-found guards become `2` (fatal); injected-string `sys.exit(1)` sites and `download_pretraineds.py` are exempt.\n\n**Files:**\n- Modify: all `patches/patch_*.py` exit sites (full corrected inventory in the plan — note: 6 previously mis-classified usage guards KEEP 1), `build_macos.py:941-977` (the patch loop), `CLAUDE.md` (convention text)\n- Test: `tests/test_patcher_exit_codes.py` (NEW)\n\n**Acceptance Criteria:**\n- [ ] Every `patches/patch_*.py` exits `2` on anchor miss and `0` on patched/already; usage guards still exit `1`; docstring Exit-codes lines updated.\n- [ ] `build_macos.py`'s patch loop: exit `0` tolerated; ANY other code collected and the build FAILS after the loop naming every failing patcher, pointing at CLAUDE.md's \"Re-pointing patches after an upstream sync\"; the two silent SKIPPED paths (patcher/source not found) are ALSO collected as failures; stale `# 0 = patched, 1 = already patched` comment replaced.\n- [ ] CLAUDE.md documents the new convention.\n- [ ] Behavioral test: 3 representative patchers on temp copies → pristine 0, re-run 0, mutated anchor 2. CASES (verified): patch_progress_routes (file-type, app.py, anchor `prevent_thread_lock=` ×1), patch_web_a11y_payload (file-type, app.py, `def launch_gradio(` ×1), patch_train_paths (dir-type → tmp/train.py, `current_dir = os.getcwd()` ×1). patch_browse_buttons is NOT a case — its REPO-relpath guard rejects off-repo targets.\n- [ ] Source-scan test: no `sys.exit(1)` remains in `patches/patch_*.py` except usage guards, the two injected-string sites, and `download_pretraineds.py`.\n- [ ] `venv_macos/bin/python build_macos.py --help` still exits 0.\n\n**Benign-nonzero proof (fail-on-nonzero is safe):** simulated 2026-08-21 — every registrable patcher run against a /tmp copy of its pristine source AND re-run on the patched output returned 0 in both cases (27 simulable registrations; browse_buttons excluded by its relpath guard but proven by every passing build). No patcher legitimately exits nonzero in a healthy build.\n\n**Verify:** `venv_macos/bin/python tests/test_patcher_exit_codes.py && venv_macos/bin/python build_macos.py --help` → `(2).` pass line + exit 0.\n\n```json:metadata\n{\"files\": [\"patches/\", \"build_macos.py\", \"CLAUDE.md\", \"tests/test_patcher_exit_codes.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_patcher_exit_codes.py && venv_macos/bin/python build_macos.py --help\", \"acceptanceCriteria\": [\"miss exits 2 everywhere, usage guards keep 1\", \"build fails on any nonzero + missing patcher/source, listing failures\", \"behavioral (3 verified cases, both arg conventions) + source-scan tests pass\", \"--help exits 0\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 7: i18n-wrap native English clusters",
      "status": "pending",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** Route the last hardcoded native-side English strings through `applio_i18n.native_tr` so the overrides file can translate them: loading-screen stages (macos_wrapper), dashboard status strings, About alert text, and dynamic process-status titles (applio_launcher).\n\n**Files:**\n- Modify: `macos_wrapper.py:1173-1372` (loading stages), `applio_launcher.py:1189, 2047-2049, 2201-2218, 2231, 3365, 3567-3619, 3844-3851, 5797-5826`\n- Test: `tests/test_applio_i18n.py` (8 → 9 after Task 1)\n\n**Acceptance Criteria:**\n- [ ] Every listed literal is wrapped so `native_tr` receives it (or its format template) at render/call time — lazy `import applio_i18n` / `_t = applio_i18n.native_tr` inside the function (pattern at `applio_launcher.py:5836-5838`; macos_wrapper already uses the same shape at :402-416). Status-map VALUES translated where the map is built (raw statuses stay KEYS); 'failed' and 'error' share ONE translated word. Also wrap the literals the ranges contain beyond the enumerated list (\"Resume\", \"Status: Paused\", \"Status: Completed\", \"Status: Error controlling process\", \"Unknown\"); do NOT wrap `_add_log_line` or `_announce_for_accessibility` payloads (they flow through AnnouncementPolicy's own translator at applio_launcher.py:4777-4780).\n- [ ] With no overrides file, behavior is byte-identical to today.\n- [ ] New cluster-keys test passes: (a) overrides translation of sample cluster strings via native_tr; (b) source scan that every listed literal stays verbatim in its owning file; (c) WRAPPED-FORM scan (`_t(\"…\")` snippets for representative sites) — the genuine pre-implementation failing signal. (A `_t(`-count threshold does NOT work: macos_wrapper.py already has 17 `_t(` matches and applio_launcher.py 12 from earlier phases.)\n- [ ] Suites: `test_applio_i18n.py` (9); `test_applio_a11y.py` (17); `test_menu_spec.py` (15).\n\n**Verify:** `venv_macos/bin/python tests/test_applio_i18n.py && venv_macos/bin/python tests/test_applio_a11y.py` → `(9)` and `(17)`.\n\n```json:metadata\n{\"files\": [\"macos_wrapper.py\", \"applio_launcher.py\", \"tests/test_applio_i18n.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_applio_i18n.py && venv_macos/bin/python tests/test_applio_a11y.py && venv_macos/bin/python tests/test_menu_spec.py\", \"acceptanceCriteria\": [\"all cluster literals routed through native_tr (wrapped-form scan is the failing signal)\", \"no-op without overrides\", \"About alert keeps the untranslated Version line first\", \"suites (9)+(17)+(15) pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 7,
      "subject": "Task 8: Docs — audit §7, CHANGELOG, CLAUDE.md",
      "status": "pending",
      "blockedBy": [
        0,
        1,
        2,
        3,
        4,
        5,
        6
      ],
      "description": "**Goal:** Record Phase 3 (fork-local) delivery in the three docs that track it, and stage the upstream-program pointer for the post-pass plan.\n\n**Files:**\n- Modify: `ACCESSIBILITY_AUDIT.md` (§7), `CHANGELOG.md` ([Unreleased]), `CLAUDE.md` (a11y section tail + test list)\n\n**Acceptance Criteria:**\n- [ ] Audit §7 \"Phase 3 additions\": each of the 5 bullets annotated DELIVERED with its commit hash; Phase 2 deferrals log-tail + typed-path marked delivered (fork side); new note: upstream Applio/gradio PR program pending the manual VoiceOver pass, own plan, gate quote included.\n- [ ] CHANGELOG `[Unreleased]` gains a Phase 3 section listing all 7 deliverables + the patcher exit-code convention change (behavior change for maintainers).\n- [ ] CLAUDE.md: Phase 3 (fork-local) paragraph; Task 6's convention text cross-checked; test list extended with `test_a11y_js_invariants.py` + `test_patcher_exit_codes.py`.\n- [ ] No upstream files touched; diff shows exactly the three doc files.\n\n**Verify:** `git diff --stat` shows exactly the three doc files; `rg -n \"Phase 3\" ACCESSIBILITY_AUDIT.md | tail -5` shows DELIVERED annotations.\n\n```json:metadata\n{\"files\": [\"ACCESSIBILITY_AUDIT.md\", \"CHANGELOG.md\", \"CLAUDE.md\"], \"verifyCommand\": \"git diff --stat && rg -n 'Phase 3' ACCESSIBILITY_AUDIT.md | tail -5\", \"acceptanceCriteria\": [\"5 audit bullets DELIVERED with hashes\", \"CHANGELOG Phase 3 section\", \"CLAUDE.md updated\", \"only 3 doc files in diff\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 8,
      "subject": "Task 9: Full chain + cert-free build validation",
      "status": "pending",
      "blockedBy": [
        7
      ],
      "description": "**Goal:** Prove the branch is regression-free end to end: every suite green with the new counts, a cert-free frozen build completes with all patchers exiting 0, the bundle carries the updated payload, and the running app serves the progress route with the new `errors` key.\n\n> **USER-ORDERED GATE — NON-SKIPPABLE.** This task enforces the user's ruling from the current conversation (2026-08-21, verbatim): \"no PR until we have everything 100% test, verified, and confirmed working with 0 regression bug.\" It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured (suite pass lines, BUILD COMPLETE, stat timestamp, curl JSON).\n\n**Files:**\n- No source modifications (validation only). Build artifacts under `build/`, `dist/` (gitignored).\n\n**Acceptance Criteria:**\n- [ ] All 9 suites green: test_applio_a11y (17), test_menu_spec (15), test_native_picker (3), test_browse_ui (9), test_progress_api (11), test_patch_fixtures (5), test_applio_i18n (9), test_a11y_js_invariants (2), test_patcher_exit_codes (2), plus `-m pytest tests/test_inference_progress.py` (5) — 78 total (17+15+3+9+11+5+9+2+2+5; adjust/report if a count legitimately shifted).\n- [ ] `venv_macos/bin/python build_macos.py` (cert-free) completes with `BUILD COMPLETE` as the FINAL command of a background run (no trailing echo masking exit); log shows every `Patching:` line, no `PATCH FAILURE`.\n- [ ] Post-build: no upstream files left with patch markers (`git checkout -- assets core.py rvc tabs app.py` if dirty — known `tabs/train/train.py` behavior).\n- [ ] Bundle: `stat -f \"%Sm\" dist/Applio.app/Contents/MacOS/Applio` AFTER last commit; `dist/Applio.app/Contents/Resources/assets/applio_a11y.js` contains `failedTail` and `#browse-record_audio_path`.\n- [ ] Boot smoke: launch `dist/Applio.app`, poll until the backend answers (the port is FIXED at 127.0.0.1:6969 — macos_wrapper.py:1167-1168; no log scraping; macOS has no `timeout` command, use a bash retry loop), `curl -s \"http://127.0.0.1:6969/applio-a11y/progress?client=web\"` returns JSON containing `\"errors\"`, quit via `osascript -e 'tell application \"Applio\" to quit'`. If 6969 never responds, check `~/Library/Logs/Applio/applio_launcher.log` for a bind failure.\n- [ ] No pushes, no PRs, no tags (hard gate — branch stays local).\n\n**Verify:** the suite loop + build command from the plan, all green/complete, evidence captured in the report.\n\n```json:metadata\n{\"files\": [], \"verifyCommand\": \"for t in test_applio_a11y test_menu_spec test_native_picker test_browse_ui test_progress_api test_patch_fixtures test_applio_i18n test_a11y_js_invariants test_patcher_exit_codes; do venv_macos/bin/python tests/$t.py || exit 1; done && venv_macos/bin/python -m pytest tests/test_inference_progress.py -q\", \"acceptanceCriteria\": [\"all suites green with expected counts\", \"cert-free build BUILD COMPLETE, no PATCH FAILURE\", \"bundle carries failedTail + scoped selector\", \"boot smoke: route JSON contains errors\", \"no pushes/PRs/tags\"], \"modelTier\": \"standard\", \"userGate\": true, \"tags\": [\"user-gate\"], \"requireEvidenceTokens\": [[\"passed\", \"green\"], [\"BUILD COMPLETE\"]], \"failurePolicy\": \"block\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-22T11:15:00+02:00"
}