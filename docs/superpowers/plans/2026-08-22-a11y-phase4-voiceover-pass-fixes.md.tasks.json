{
  "planPath": "docs/superpowers/plans/2026-08-22-a11y-phase4-voiceover-pass-fixes.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: Announce mode Auto/Native + diagnostic",
      "status": "completed",
      "description": "**Goal:** Replace the silently-broken default (in-app announcements owned by window-level NSAccessibility posts, unheard across 3 builds) with an \"Auto\" default routing in-app speech through the web live region, keep \"Native\" as an opt-in menu mode, and add one log line that makes the native path diagnosable forever.\n\n**Files:**\n- Modify: `applio_launcher.py:4841-4852` (owner set in `__init__`), `:5493-5518` (`_a11y_post`), settings setters + `_push_a11y_settings`, `menu_spec.py:77-83` (A11Y_CHILD_KEYS) + Accessibility submenu items, `applio_progress_api.py` settings echo\n- Test: `tests/test_menu_spec.py` (15 → 17), `tests/test_progress_api.py` (11 → 12)\n\n**Acceptance Criteria:**\n- [ ] Persisted `a11y.announce_mode` ∈ {\"auto\",\"native\"}, default \"auto\", two new submenu items (a11y.announce.auto \"Announcements: Auto (recommended)\", a11y.announce.native \"Announcements: Native (experimental)\"), checkmark on active, persisted like a11y.verbosity.\n- [ ] Mode→owner: auto → set_announce_owner(\"web\"); native → set_announce_owner(\"native\"); applied at __init__ and on change.\n- [ ] `_a11y_post` under auto: SKIPS applio_a11y.post_announcement while KEEPING the terminal attention/sound block for BOTH modes (restructured so the conditional post does not gate it); under native: unchanged. One instrumentation log line (mode, isVoiceOverEnabled, element) before the decision, exception-guarded.\n- [ ] Payload settings echo carries announce_mode (_push_a11y_settings payload gains the key; _state[\"settings\"] DEFAULT gains it too so the echo exists before the first push); per-request owner rule UNTOUCHED (external browsers unaffected). Known layering under Auto: the in-app JS announces job start/terminal via its live region alongside the toasts (Global Constraint 4) — judgment item in the Task 6 re-test checklist.\n- [ ] Suites: menu_spec (17), progress_api (12), applio_a11y (17) green.\n\n**Verify:** `venv_macos/bin/python tests/test_menu_spec.py && venv_macos/bin/python tests/test_progress_api.py && venv_macos/bin/python tests/test_applio_a11y.py` → (17)/(12)/(17).\n\n```json:metadata\n{\"files\": [\"applio_launcher.py\", \"menu_spec.py\", \"applio_progress_api.py\", \"tests/test_menu_spec.py\", \"tests/test_progress_api.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_menu_spec.py && venv_macos/bin/python tests/test_progress_api.py && venv_macos/bin/python tests/test_applio_a11y.py\", \"acceptanceCriteria\": [\"announce_mode setting + 2 menu items persisted\", \"auto skips native post, keeps attention/sound/log\", \"diagnostic log line present\", \"settings echo carries announce_mode\", \"suites (17)/(12)/(17)\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 2: patch_job_toasts.py lifecycle toasts",
      "status": "completed",
      "description": "**Goal:** The jobs that start/finish silently get announced through gradio toasts (the VoiceOver-proven channel), injected at build time via a new multi-target patcher modeled on patch_stop_feedback.py — the code that later becomes the upstream Applio PR.\n\n**Files:**\n- Create: `patches/patch_job_toasts.py`\n- Modify: `build_macos.py` patches_to_apply (+4 \"dir\" tuples; NEVER import it — verify via --help)\n- Test: `tests/test_patch_fixtures.py` (+1 → 6), `tests/test_patcher_exit_codes.py` (CASES +1, count stays 2)\n\n**Acceptance Criteria:**\n- [ ] Injection per the plan's verbatim spec: inference.py enforce_terms (start + finish + error; marker _APPLIO_TOASTS_INFERENCE); tts.py enforce_terms (same shape, _APPLIO_TOASTS_TTS); train.py enforce_terms START-only (_APPLIO_TOASTS_TRAIN_START) + two wrapper defs (_applio_preprocess_toast/_applio_extract_toast) injected at the HEAD of train_tab()'s body (NOT above def enforce_terms — the fn= rewires at lines 502/597 execute BEFORE line 754 is reached; wrong placement = UnboundLocalError at app startup), markers _APPLIO_TOASTS_TRAIN_PRE/_APPLIO_TOASTS_TRAIN_EXT, failure predicate `\"error\" in s.lower() or \"failed\" in s.lower()` (core's failures read \"Preprocessing failed…\"/\"Feature extraction failed…\" — startswith(\"error\") never matches); download.py wrapper at the head of download_tab() for fn=run_download_script (_APPLIO_TOASTS_DOWNLOAD). enforce_terms_batch gets NO toasts (Task 3 owns batch). Existing toasts (download.py:146/161, train.py:861-872) untouched.\n- [ ] Patcher: TARGETS multi-target, apply() resolves file- or dir-type args, ABSENT targets continue (non-fatal — single-file temp test exits 0), per-sub-patch OWN markers (SIX distinct), exit 0 patched/already + 2 on miss of an existing file, usage guard 1.\n- [ ] Double-run → already/0; mutated anchor temp copy → 2; patched outputs py_compile clean; fixture test asserts wrapper defs syntactically PRECEDE the rewired fn= lines.\n- [ ] Registered (4 tuples); --help exits 0; fixture test +1 (injected lines verbatim per file); exit-codes CASES + patch_job_toasts; tree restored clean after tests.\n\n**Verify:** `venv_macos/bin/python tests/test_patch_fixtures.py && venv_macos/bin/python tests/test_patcher_exit_codes.py && venv_macos/bin/python build_macos.py --help` → (6)/(2)/help+0.\n\n```json:metadata\n{\"files\": [\"patches/patch_job_toasts.py\", \"build_macos.py\", \"tests/test_patch_fixtures.py\", \"tests/test_patcher_exit_codes.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_patch_fixtures.py && venv_macos/bin/python tests/test_patcher_exit_codes.py && venv_macos/bin/python build_macos.py --help\", \"acceptanceCriteria\": [\"all 4 files injected per spec, batch wrapper toast-free\", \"idempotent + miss exits 2\", \"registered, --help exits 0\", \"fixture + exit-code tests green\", \"tree clean\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 3: Batch toasts engine-side",
      "status": "pending",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** The batch flow gets start, 25/50/75% milestone, completion/cancelled, and error toasts — all inside the fork-owned INFER_BATCH_REPLACEMENT body of patch_inference_progress.py (verified: gradio contextvars live on the handler thread mid-loop; toasts deliver immediately via SSE).\n\n**Files:**\n- Modify: `patches/patch_inference_progress.py` — INFER_PROGRESS_HELPERS (+_infer_toast), INFER_BATCH_REPLACEMENT (loop entry ~L166, terminal ~L208-218, error ~L226-244)\n- Test: `tests/test_inference_progress.py` (pytest, 5 → 7)\n\n**Acceptance Criteria:**\n- [ ] `_infer_toast(msg)` helper: lazy `import gradio as gr; gr.Info(msg)` in try/except with print fallback; gradio NEVER imported at top of generated engine code.\n- [ ] Start after the counters, before the initial progress write: `_infer_toast(f\"Batch conversion started: {total} files\")`.\n- [ ] Milestones: `_next_milestone = 25` with the counters; after the per-file progress write at the loop tail, when `total >= 8` AND `processed < total` AND `processed * 100 // total >= _next_milestone`: toast `f\"{processed}/{total} files converted ({_next_milestone}%)\"`, then `_next_milestone += 25`. Threshold-first-crossing semantics (label = threshold); the processed<total guard suppresses the 100% milestone so it never double-announces with the terminal toast; small batches: start+terminal only.\n- [ ] Terminal block (after the terminal progress write, before _infer_add_to_history): `_infer_toast(f\"Batch conversion {status}: {converted} converted, {skipped} skipped in {elapsed:.0f}s\")`; ERROR toasts at TWO sites: in the except handler before the existing raise using `str(_infer_exc)` (the variable IS `_infer_exc`, not `e`), AND immediately before the concurrent-run `raise RuntimeError(...)` (that raise sits BEFORE the body's try: — the except-block toast can never cover it). Tab-side except stays toast-free.\n- [ ] pytest +2: milestone/terminal/BOTH error toast calls present in generated output (incl. the pre-try concurrent-run site); `import gradio` only inside _infer_toast.\n\n**Verify:** `venv_macos/bin/python -m pytest tests/test_inference_progress.py -q` → `7 passed`.\n\n```json:metadata\n{\"files\": [\"patches/patch_inference_progress.py\", \"tests/test_inference_progress.py\"], \"verifyCommand\": \"venv_macos/bin/python -m pytest tests/test_inference_progress.py -q\", \"acceptanceCriteria\": [\"start/milestone/terminal/error toasts in injected body\", \"milestones gated to total>=8, 25% steps\", \"lazy gradio import only\", \"7 passed\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 4: Active-Processes menu with progress",
      "status": "pending",
      "description": "**Goal:** Fix the never-populates bug (menu fed only get_active_processes(); in-process batch synthesized elsewhere) and upgrade titles to live progress (\"Inference: batch — 43% (2/3 files)\", \"Training: voice — epoch 34/200\"), unifying the live-proc merge the dashboard uses.\n\n**Files:**\n- Modify: `applio_launcher.py` — module-level `_merged_live_procs()` + `_menu_job_title(proc, training_epoch=None)`; `_update_menu_state:5314` + stale comment 5310-5312; `_refresh_status_submenu:5337-5381` rewrite (no arg); dashboard call sites 3928 + 4450 switched to the helper\n- Test: NEW `tests/test_menu_jobs.py` (4 tests; `import applio_launcher` verified importable)\n\n**Acceptance Criteria:**\n- [ ] `_merged_live_procs()` = get_active_processes() + _synthesize_inference_proc() append — used by menu AND both dashboard sites.\n- [ ] `_menu_job_title(proc, training_epoch=None)`: inference `f\"Inference: {name or 'batch'} — {pct}% ({processed}/{total} files)\"` (pct via compute_inference_stats(proc, time.time()) — synthesized proc carries total/processed/converted/started_at), cancelling variant, training `epoch {cur}/{total_epoch}` (cur = caller-supplied training_epoch, total_epoch from proc — training procs carry it; \"starting…\" when None), preprocess/extract name-only, tts \"TTS: active job\" (tracked bare), paused suffix \" — paused\" derived from proc['_ps_stopped']; native_tr at call time; name ≤30 chars, line ≤64.\n- [ ] Module-level `_last_training_metrics(log_path, max_bytes=262144)` EXTRACTED from _parse_training_metrics (controller keeps its snapshot branch and delegates — one tail-scan reader, two consumers); module-level `_annotate_paused(procs)` psutil probe (mirrors _annotate_pause_state) called by the menu path before titles.\n- [ ] FROZEN-SAFETY: never enrich_jobs/process_log_parser for the menu (frozen-unimportable).\n- [ ] `_refresh_status_submenu()` no-arg: merged list + titles, SKIPS NSMenu rebuild only when title tuple unchanged AND item.isEnabled() (a full _build_native_menu pass rebuilds the status parent DISABLED from the static spec — a titles-only skip would leave a dead submenu), placeholder + dashboard-click dispatch unchanged, existing exception guard preserved.\n- [ ] tests/test_menu_jobs.py: inference title (43%/2-of-3), training title via the training_epoch param, merged helper with monkeypatched sources, truncation + paused suffix (proc with _ps_stopped: True). test_menu_spec still (17).\n\n**Verify:** `venv_macos/bin/python tests/test_menu_jobs.py && venv_macos/bin/python tests/test_menu_spec.py` → (4)/(17).\n\n```json:metadata\n{\"files\": [\"applio_launcher.py\", \"tests/test_menu_jobs.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_menu_jobs.py && venv_macos/bin/python tests/test_menu_spec.py\", \"acceptanceCriteria\": [\"merged helper used by menu + both dashboard sites\", \"progress titles per format spec\", \"frozen-safe enrichment only\", \"skip-rebuild when unchanged\", \"suites (4)/(17)\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 5: Dashboard readability (AX values)",
      "status": "pending",
      "description": "**Goal:** Make the Process Dashboard readable by a blind user: rows announce metrics, progress announces template text, the loss chart's value summarizes the curve, Tab deterministically exits the runs list, and selecting a row announces its summary.\n\n**Files:**\n- Modify: `applio_launcher.py` — module-level `_row_ax_summary(proc, metrics=None, total_epoch=None, eta=None)`; ProcessDashboardController: new `tableView_didAddRowView_forRow_`, progress-value templates 3190-3192/3420-3422, indeterminate branch 3194-3201, LossChartView.set_points value 2434-2447 (built at MODULE level, never a view method), key-view loop in _create_detail_panel, selection announcement in tableViewSelectionDidChange_ 3896-3916, dead status_badge branch 3045-3048 removed\n- Test: NEW `tests/test_dashboard_ax.py` (3 tests, pure builder)\n\n**Acceptance Criteria:**\n- [ ] `_row_ax_summary`: training `f\"epoch {cur} of {total}, best loss {best}, ETA {eta}\"` (best `:.4g` or \"--\" when best_loss is None — first epoch; eta \"--\" default; controller callers pass self._derive_eta(...)); inference `f\"{processed} of {total} files converted, {pct:.0f} percent\"`; unknown → \"\"; native_tr at call time.\n- [ ] Row values via a DUAL hook (the runs list is CELL-BASED — dataSource objectValue, no view factory; didAddRowView is documented for view-based tables): one `_apply_row_ax(table, row, view=None)` helper (view or rowViewAtRow_; skip when None) called from BOTH `tableView_didAddRowView_forRow_` AND `tableView_willDisplayCell_forTableColumn_row_` (the guaranteed cell-based hook; dedupe when the label is already set). RowView label \"Training run {name}\"/\"Inference batch {name}\" + Value (summary) via the dataSource row arithmetic; visible cell stays short.\n- [ ] Progress: training `\"Epoch {cur} of {total}, {int(frac*100)} percent\"`; inference `\"{processed} of {total} files, {int(pct)} percent\"`; indeterminate → \"Working, no metrics yet\" (fixes stale value).\n- [ ] Chart value: full summary (epochs plotted, range, fell-from/to, best @ epoch, significant-improvements count via the EXISTING module-level _significant_improvements at applio_launcher.py:2354) + \"no data yet\" case; module-level builder `_chart_ax_summary(points)`, never a view method.\n- [ ] Key-view loop at the END of _create_detail_panel (process_table exists by then): process_table → stop → pause → reveal → open → process_table (setNextKeyView_, once).\n- [ ] Selection announcement: gated on isVoiceOverEnabled (selector verified) + verbosity != off, post_announcement(process_table, summary + \"Detail pane updated.\"), main-thread, try/except-wrapped.\n- [ ] Dead status_badge branch deleted (verified dead: badge is ProgressWindowController-only); tests/test_dashboard_ax.py (3) green; test_applio_a11y (17) green.\n\n**Verify:** `venv_macos/bin/python tests/test_dashboard_ax.py && venv_macos/bin/python tests/test_applio_a11y.py` → (3)/(17).\n\n```json:metadata\n{\"files\": [\"applio_launcher.py\", \"tests/test_dashboard_ax.py\"], \"verifyCommand\": \"venv_macos/bin/python tests/test_dashboard_ax.py && venv_macos/bin/python tests/test_applio_a11y.py\", \"acceptanceCriteria\": [\"per-row AX labels+values\", \"template progress values + indeterminate fix\", \"chart summary value\", \"key-view loop\", \"selection announcement gated\", \"suites (3)/(17)\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 6: Docs — audit §7, CHANGELOG, CLAUDE.md",
      "status": "pending",
      "blockedBy": [
        0,
        1,
        2,
        3,
        4
      ],
      "description": "**Goal:** Record the Phase 4 fixes against the pass findings they resolve, and stage the next manual-pass checklist.\n\n**Files:**\n- Modify: `ACCESSIBILITY_AUDIT.md` (§7), `CHANGELOG.md` ([Unreleased]), `CLAUDE.md` (Phase 4 paragraph + patcher/test lists)\n\n**Acceptance Criteria:**\n- [ ] Each of the 5 pass findings annotated with its fix + commit hash; new \"re-test checklist\" for the NEXT manual pass (batch toasts on start/milestone/finish; menu shows batch % + training epoch; dashboard rows read metrics + Tab exits list; Announcements submenu shows Auto active).\n- [ ] CHANGELOG Phase 4 section (toasts, menu, dashboard AX, announce mode + Auto-default behavior change).\n- [ ] CLAUDE.md: Phase 4 paragraph (Auto semantics, batch-toast split rule, frozen-safe enrichment rule), patch_job_toasts in patcher mentions, new test files listed.\n- [ ] Exactly the three doc files in the diff.\n\n**Verify:** `git diff --stat` → 3 files; `rg -n \"Phase 4\" ACCESSIBILITY_AUDIT.md | tail -5`.\n\n```json:metadata\n{\"files\": [\"ACCESSIBILITY_AUDIT.md\", \"CHANGELOG.md\", \"CLAUDE.md\"], \"verifyCommand\": \"git diff --stat && rg -n 'Phase 4' ACCESSIBILITY_AUDIT.md | tail -5\", \"acceptanceCriteria\": [\"5 findings annotated with fixes+hashes\", \"re-test checklist added\", \"CHANGELOG + CLAUDE.md updated\", \"3 files only\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 7: Full chain + frozen build (gate)",
      "status": "pending",
      "blockedBy": [
        5
      ],
      "description": "**Goal:** Prove the branch regression-free end to end: all suites green with new counts, cert-free frozen build completes with all patchers (incl. patch_job_toasts) exiting 0, bundle carries the new code, the running app serves the progress route with announce_mode in settings, and patched tab sources ship in the bundle.\n\n> **USER-ORDERED GATE — NON-SKIPPABLE.** This task enforces the owner's standing ruling (2026-08-21, verbatim): \"no PR until we have everything 100% test, verified, and confirmed working with 0 regression bug.\" It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every acceptance criterion has been re-validated independently, with output captured.\n\n**Files:** none (validation only; artifacts gitignored).\n\n**Acceptance Criteria:**\n- [ ] All suites green: applio_a11y (17), menu_spec (17), native_picker (3), browse_ui (9), progress_api (12), patch_fixtures (6), applio_i18n (9), a11y_js_invariants (2), patcher_exit_codes (2), menu_jobs (4), dashboard_ax (3), pytest inference_progress (7) — 91 total (adjust/report legitimate shifts).\n- [ ] Cert-free build: background run, build as FINAL command, BUILD COMPLETE, every Patching: line incl. the 4 job_toasts registrations, zero PATCH FAILURE; post-build restore if dirty; tree clean.\n- [ ] Bundle: binary timestamp AFTER last commit; patched sources ship because `tabs` + `rvc` are PyInstaller DATAS (build_macos.py:582-597) — assert markers on the Resources copies (NOT Frameworks/PYZ — .pyc strips comments): rg -c _APPLIO_TOASTS_INFERENCE dist/Applio.app/Contents/Resources/tabs/inference/inference.py (+ tts/train/download equivalents) and rg -c _infer_toast dist/Applio.app/Contents/Resources/rvc/infer/infer.py all ≥1; assets/applio_a11y.js unchanged vs main (Task 1 touches no JS).\n- [ ] Boot smoke: launch dist/Applio.app, poll `curl -s \"http://127.0.0.1:6969/applio-a11y/progress?client=web\"` → JSON contains \"announce_mode\", quit via osascript.\n- [ ] No pushes, no PRs, no tags.\n\n**Verify:** the 11-suite loop + pytest + `venv_macos/bin/python build_macos.py` (final command), evidence captured.\n\n```json:metadata\n{\"files\": [], \"verifyCommand\": \"for t in test_applio_a11y test_menu_spec test_native_picker test_browse_ui test_progress_api test_patch_fixtures test_applio_i18n test_a11y_js_invariants test_patcher_exit_codes test_menu_jobs test_dashboard_ax; do venv_macos/bin/python tests/$t.py || exit 1; done && venv_macos/bin/python -m pytest tests/test_inference_progress.py -q\", \"acceptanceCriteria\": [\"all suites green (91 target)\", \"cert-free BUILD COMPLETE, all patchers exit 0\", \"bundle carries toast markers\", \"boot smoke JSON contains announce_mode\", \"no pushes/PRs/tags\"], \"modelTier\": \"standard\", \"userGate\": true, \"tags\": [\"user-gate\"], \"requireEvidenceTokens\": [[\"passed\", \"green\"], [\"BUILD COMPLETE\"]], \"failurePolicy\": \"block\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-22T20:35:00+02:00"
}